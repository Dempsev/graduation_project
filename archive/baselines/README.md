# Baselines

This directory collects historical or comparison workflows that remain valuable as baselines after the prediction / optimization split.

In phase 1, the most important baseline is:

- `legacy_stage3_training/`

Use this directory to keep older mixed pipelines readable and available for comparison without treating them as the current thesis-ready mainline.
