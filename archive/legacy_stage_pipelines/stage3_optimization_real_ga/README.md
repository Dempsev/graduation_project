# Legacy Real-GA Compatibility Layer

The primary real-GA implementation has moved to:

- `optimization/real_comsol_ga/`

This directory is now kept so older MATLAB paths and notes do not break during the staged refactor.
