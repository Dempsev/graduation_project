# Legacy Prediction Compatibility Layer

The primary prediction implementation has moved to:

- `prediction/dataset/`
- `prediction/models/`

This directory is now kept as a compatibility layer so older paths and notes do not break during the staged refactor.
