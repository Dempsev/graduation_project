# Moved Package

`prediction_targetband_param_v1` has been migrated to
`src/prediction/targetband_param` as part of the final public refactor.

The root package is kept only as an import-compatibility shim. Use the public
entry scripts under `scripts/` for reproducible runs.
