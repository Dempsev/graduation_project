# Legacy Optimization Compatibility Layer

The primary surrogate-assisted optimization implementation has moved to:

- `optimization/seed_ranking/`

This directory is now kept as a compatibility layer so older Python paths and notes do not break during the staged refactor.
