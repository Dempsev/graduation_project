# Analysis Layer

This package is for post-hoc analysis helpers shared by experiment and robustness scripts.

Use this layer for:

- objective-to-metric resolution
- shared plotting or ranking helpers
- analysis-only utilities that should not leak into training core
